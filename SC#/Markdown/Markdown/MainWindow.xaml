<Window x:Class="Markdown.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Markdown Editörü" Height="720" Width="1200"
        Background="White"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Menü ve açılır liste (dropdown) stili -->
        <Style x:Key="MenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6,3"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#0078D7"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Menu">
            <Setter Property="ItemContainerStyle" Value="{StaticResource MenuItemStyle}"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Üst Menü -->
        <Menu DockPanel.Dock="Top" Background="#EAEAEA" Foreground="Black" FontWeight="SemiBold">
            <MenuItem Header="📂 File">
                <MenuItem Header="Open..." Click="OpenFile_Click"/>
                <MenuItem Header="Save" Click="SaveFile_Click"/>
                <MenuItem Header="Export to HTML" Click="ExportHtml_Click"/>
                <Separator/>
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="✏️ Edit">
                <MenuItem Header="Undo" Click="Undo_Click"/>
                <MenuItem Header="Redo" Click="Redo_Click"/>
            </MenuItem>
            <MenuItem Header="👁️ View">
                <MenuItem Header="Toggle Preview" Click="TogglePreview_Click"/>
            </MenuItem>
            <MenuItem Header="➕ Insert">
                <MenuItem Header="Bold" Click="Bold_Click"/>
                <MenuItem Header="Italic" Click="Italic_Click"/>
                <MenuItem Header="Heading" Click="Heading_Click"/>
            </MenuItem>
        </Menu>

        <!-- Ana içerik -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Markdown TextBox -->
            <Border Grid.Column="0" Margin="10" CornerRadius="6" BorderBrush="#CCCCCC" BorderThickness="1" Background="White" >
                <TextBox x:Name="MarkdownInput" 
                         Background="White" 
                         Foreground="Black" 
                         FontFamily="Consolas" 
                         FontSize="16"
                         AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto" 
                         TextChanged="MarkdownInput_TextChanged"
                         BorderThickness="0"
                         Padding="10"
                         SpellCheck.IsEnabled="False"/>
            </Border>

            <!-- Bölücü -->
            <GridSplitter Grid.Column="1" Width="5" Background="#CCCCCC" HorizontalAlignment="Stretch" />

            <!-- Önizleme -->
            <Border Grid.Column="2" Margin="10" CornerRadius="6" BorderBrush="#CCCCCC" BorderThickness="1" Background="White">
                <wv2:WebView2 x:Name="PreviewBrowser" Margin="0"/>
            </Border>
        </Grid>
    </DockPanel>
</Window>
